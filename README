Cadence GitHub Pages

    http://bigeasy.github.io/cadence/

You will need to install Pygments for syntax highlighting.

http://pygments.org/

Then:

$ npm install
$ node serve.js

And then you can:

$ open http://127.0.0.1:3000

Now, whenever you refresh your browser, `serve.js` will run `make` and update
the pages.
